# AI Provider (Fireworks)
FIREWORKS_API_KEY=your_fireworks_api_key_here

# Database (PostgreSQL)
POSTGRES_URL=postgres://localhost:5432/privy

# Auth.js Secret (generate with: openssl rand -base64 32)
AUTH_SECRET=your_auth_secret_here

# IP Hashing Salt (REQUIRED in production)
# Used for privacy-preserving IP rate limiting (5 accounts per IP per 24h)
# Generate with: openssl rand -base64 32
IP_SALT=your_random_salt_here

# Encryption Master Salt (REQUIRED in production)
# Additional salt for message encryption key derivation (defense in depth)
# Generate with: openssl rand -base64 32
ENCRYPTION_MASTER_SALT=your_encryption_master_salt_here

# Logfire Observability (OpenTelemetry)
# Get your token from: https://logfire.pydantic.dev
LOGFIRE_TOKEN=your_logfire_write_token
OTEL_EXPORTER_OTLP_ENDPOINT=https://logfire-api.pydantic.dev
OTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer ${LOGFIRE_TOKEN}

# Optional: Vercel Blob for file storage
BLOB_READ_WRITE_TOKEN=

# Optional: Redis for rate limiting
REDIS_URL=

# Optional: Stripe for payments
STRIPE_SECRET_KEY=here_you_can_add_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=here_you_can_add_your_stripe_webhook_secret
STRIPE_PUBLISHABLE_KEY=here_you_can_add_your_stripe_publishable_key
STRIPE_PRICE_ID=price_xxx  # Premium monthly subscription price ID from Stripe dashboard

# Mixpanel Analytics
NEXT_PUBLIC_MIXPANEL_TOKEN=your_mixpanel_project_token
NEXT_PUBLIC_MIXPANEL_DEBUG=false  # Set to true for debug logging

# Cron Job Security (REQUIRED in production)
# Used to secure the /api/cron/cleanup endpoint
# Generate with: openssl rand -base64 32
# Vercel cron jobs will automatically use this secret
CRON_SECRET=your_random_cron_secret_here